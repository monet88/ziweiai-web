interface:
  display_name: "Ziweiai Web"
  short_description: "Repo-specific patterns and workflows for ziweiai-web"
  default_prompt: "Use the ziweiai-web repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true