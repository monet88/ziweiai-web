<script lang="ts">
  // Route Hợp Hôn (US-017c): wrapper mỏng cho màn ghép đôi. Cờ EXTENDED_SYSTEM_HEPAN gate ở
  // backend (POST /pairings); route luôn render UI, submit mới chạm gate.
  import HepanScreen from '$lib/features/hepan/HepanScreen.svelte';
</script>

<HepanScreen />
