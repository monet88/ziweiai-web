# Harness durable layer — each project instance generates its own data.
harness.db
harness.db-wal
harness.db-shm

# macOS local metadata.
.DS_Store

# Rust build output for harness tooling.
target/

# Downloaded Harness CLI binary for installed project instances.
scripts/bin/harness-cli
scripts/bin/harness-cli.exe

# ---------------------------------------------------------------------------
# AI agent tooling — ignore junk / logs / cache; KEEP durable skills & config.
# ---------------------------------------------------------------------------
# Full local agent homes (session state, caches, private config).
.claude/
.codex/
.gemini/
.agent/
agent/
.kiro
.codegraph/
.gitnexus/
.playwright-mcp/
.codepilot-uploads/
.headroom/
.serena/
.vscode/
mcps/

# .agents: ignore transient dirs only — skills/workflows/rules stay trackable.
.agents/logs/
.agents/cache/
.agents/tmp/
.agents/**/logs/
.agents/**/cache/
.agents/**/tmp/
!.agents/skills/
!.agents/skills/**
!.agents/workflows/
!.agents/workflows/**
!.agents/rules/
!.agents/rules/**
!.agents/AGENTS.md

# Impeccable skill scratch TODO list (local-only).
/TODOS.md

# External reference repositories for code study/reuse.
.ref/

# JavaScript / TypeScript workspace artifacts.
node_modules/
pnpm-lock.yaml.tmp
.turbo/
.svelte-kit/
dist/
build/
coverage/
out/
.next/
.vercel

# Playwright E2E output.
test-results/
playwright-report/
.playwright/

# Playwright E2E output.
test-results/
playwright-report/
.playwright/

# Temp tooling output.
whyzod.txt
.tmp-review/

# HTML UI preview reference (không phải nguồn build) + báo cáo review nội bộ.
templates/ui-reference/
plans/reports/
tmp/
temp/
temp-envs/

# HTML UI preview reference (không phải nguồn build) + báo cáo review nội bộ.
templates/ui-reference/
plans/reports/

# ---------------------------------------------------------------------------
# Environment / secrets / credentials — never commit real values.
# ---------------------------------------------------------------------------
.env
.env.*
*.env
*.env.*
!**/.env.example
*.local
*.secret
*.secrets
*.credentials
*credentials*.json
*credential*.json
*service-account*.json
*service_account*.json
firebase-adminsdk*.json
google-services.json
GoogleService-Info.plist
*.pem
*.key
*.p12
*.p8
*.keystore
*.jks
*.mobileprovision
*.cer
*.crt
*.token
**/secrets/
**/credentials/

# Supabase local temp / link state (may contain project refs).
apps/api/supabase/.temp/
apps/api/supabase/.branches/
supabase/.temp/
**/.temp/

# Deploy SSH keys — never commit private keys.
docs/deploy/*.pem
docs/deploy/*.key
docs/deploy/lightsail-ssh-key.txt

# Root agent instruction files (local-only, not per-folder AGENTS.md).
/CLAUDE.md
/AGENTS.md
AGENTS.md.orig.bak
.mcp.json
# ---------------------------------------------------------------------------
# Large binaries / release media / debug dumps — never push to GitHub.
# ---------------------------------------------------------------------------
*.apk
*.aab
*.ipa
*.mp4
*.mov
*.m4v
*.zip
*.7z
*.rar
*.tar
*.tar.gz
*.tgz
*.dmg
*.iso
ziweiai-demo.zip
**/logcat.txt
**/window_dump.xml
*.log
logs/

# Local-only root agent scratch files.
/CLAUDE.md
AGENTS.md.orig.bak
.mcp.json
galatuvi

# Additional explicit ignores for service keys requested by user
*service-key*.json
*service_key*.json
*service_key*.pem
*service-key*.pem
