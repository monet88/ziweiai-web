# Chỉ PUBLIC_* được lộ ra client bundle (docs/product/invariants.md §1).
# Tuyệt đối KHÔNG đặt secret server (SERVICE_ROLE_KEY, *_API_KEY, JWT secret) ở đây.
PUBLIC_API_BASE_URL=http://localhost:3000
PUBLIC_SUPABASE_URL=https://your-project.supabase.co
PUBLIC_SUPABASE_ANON_KEY=your-anon-key
